#include <stdio.h>
void main()
{

        int n, sum = 0, k, temp, c;
        n = 153 = temp;
        while (n >= 0)
        {
                k = n%10;
                c = k*k*k;
                sum += c;
        }
}